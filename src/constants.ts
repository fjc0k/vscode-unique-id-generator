export const UUID_GENERATE = 'uuid.generate'
export const UUID_REPEAT = 'uuid.repeat'

export const CUID_GENERATE = 'cuid.generate'
export const CUID_REPEAT = 'cuid.repeat'

export const NANOID_GENERATE = 'nanoid.generate'
export const NANOID_REPEAT = 'nanoid.repeat'

export const Commands: string[] = [
  UUID_GENERATE,
  UUID_REPEAT,
  CUID_GENERATE,
  CUID_REPEAT,
  NANOID_GENERATE,
  NANOID_REPEAT,
]
